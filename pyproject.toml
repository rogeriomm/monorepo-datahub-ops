[tool.poetry.dependencies]
python = ">=3.10"
datacontract-cli = { version = "*", extras = ["all"] }
pyspark = "^3.4.0"
rich = "^13.0.0"
openbrewerydb = "^0.1.1"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
package-mode = false
